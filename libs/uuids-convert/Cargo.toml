[package]
name = "uuids-convert"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
persistence = { workspace = true, features = ["help-crates"] }
uuid.workspace = true
